# Desafio 03
# Crie um scrypt python que leia dois números e tente mostrar a soma entre eles.
primeiro = float(input('Digite o primeiro número: '))
segundo = float(input('Digite o segundo número: '))
soma = primeiro+segundo
print('A soma entre', primeiro, 'e', segundo, 'é igual a:', soma)